extends layout

block content
  .grid_12
    #peopleMain
      ul
        for person in people
          li
            a(href='/people/#{person.id}') #{person.name.first} #{person.name.last} 
              if person.admin
                | (admin)